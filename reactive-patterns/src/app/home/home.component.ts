import { Component } from '@angular/core';

// We use the selector like we would an HTML element in view templates that should render this component
@Component({
  selector: 'app-home',
  templateUrl: './home.component.html',
})
export class HomeComponent {
}
